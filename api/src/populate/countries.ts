import { INode } from "../model/INode";

export const countries: INode[] = [
    { 
        labels: ['Country'], 
        properties: { name: 'US' } 
    },
    { 
        labels: ['Country'], 
        properties: { name: 'UK' } 
    },
    { 
        labels: ['Country'], 
        properties: { name: 'Canada' } 
    },
];